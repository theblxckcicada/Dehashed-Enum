```
usage: dehashed.py [-h] -a API_KEY -u USERNAME [-d DOMAIN] [-e EMAIL] [-f EMAIL_FILE] [-o OUTPUT_FILE]

Script description

options:
  -h, --help            show this help message and exit
  -a API_KEY, --api_key API_KEY
                        Dehashed api key
  -u USERNAME, --username USERNAME
                        Dehashed username subscription email
  -d DOMAIN, --domain DOMAIN
                        Target domain to query on dehashed.com
  -e EMAIL, --email EMAIL
                        Email to query on dehashed.com
  -f EMAIL_FILE, --email_file EMAIL_FILE
                        File containing email list
  -o OUTPUT_FILE, --output_file OUTPUT_FILE
                        Directory to store your results, default=current directory
```